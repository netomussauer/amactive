# ── Python (apps/api) ──────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
.env
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/

# Uploads locais de imagem de produto (volume Docker em produção/dev via
# compose — ver docker-compose.yml `amactive_uploads_data`; este caminho só
# é usado quando a API roda fora do Docker, apontando para o default de
# `Settings.uploads_dir`).
apps/api/uploads/

# ── Node / Web (apps/web) ──────────────────────────────────────
node_modules/
apps/web/dist/
apps/web/.vite/
apps/web/*.tsbuildinfo
apps/web/vite.config.js
apps/web/vite.config.d.ts
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Editor / OS ─────────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# ── Docker ────────────────────────────────────────────────────
docker-compose.override.yml
